name: Apply image content validation

on:
  push:
    branches:
      - refactor/stable-analysis-ids-trace-elements-image-wizard
    paths:
      - tools/patch_image_validation.py
      - .github/workflows/apply-image-validation.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply deterministic patch
        run: python tools/patch_image_validation.py
      - name: Validate syntax
        run: python -m py_compile petrolab/services/image_service.py
      - name: Commit patch and remove one-shot tooling
        shell: bash
        run: |
          git config user.name "PetroLab Refactor"
          git config user.email "actions@users.noreply.github.com"
          git add petrolab/services/image_service.py
          git rm tools/patch_image_validation.py .github/workflows/apply-image-validation.yml
          git commit -m "Validate image content before storage"
          git push
