name: Apply Fe2O3t formula guard

on:
  push:
    branches:
      - refactor/stable-analysis-ids-trace-elements-image-wizard
    paths:
      - tools/patch_formula_fe2o3t_guard.py
      - .github/workflows/apply-formula-fe2o3t-guard.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply deterministic patch
        run: python tools/patch_formula_fe2o3t_guard.py
      - name: Validate syntax
        run: python -m py_compile petrolab/minerals/formulae.py
      - name: Commit patch and remove one-shot tooling
        shell: bash
        run: |
          git config user.name "PetroLab Refactor"
          git config user.email "actions@users.noreply.github.com"
          git add petrolab/minerals/formulae.py
          git rm tools/patch_formula_fe2o3t_guard.py .github/workflows/apply-formula-fe2o3t-guard.yml
          git commit -m "Reject total Fe2O3 in structural formula input"
          git push
